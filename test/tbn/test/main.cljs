(ns tbn.test.main
  (:require [tbn.test.collection :as coll]
            [tbn.test.commands   :as cmd]
            [tbn.test.model      :as m]
            [tbn.test.store      :as store]))

;; need to set the main fn to get a compiling test suite
;; (set! *main-cli-fn* (fn []))
